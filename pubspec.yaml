name: smart_crop_assistant
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  camera: ^0.11.0
  cloud_firestore: ^6.2.0
  crypto: ^3.0.7
  firebase_auth: ^6.3.0
  firebase_core: ^4.6.0
  firebase_messaging: ^16.1.3
  firebase_storage: ^13.3.0
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  flutter_tts: ^4.2.5
  geocoding: ^4.0.0
  geolocator: ^14.0.1
  google_fonts: ^8.0.2
  http: ^1.6.0
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  pdf: ^3.12.0
  printing: ^5.14.3
  provider: ^6.1.5+1
  speech_to_text: ^7.3.0
  tflite_flutter: ^0.10.4
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/crops.json
    - assets/images/
    - assets/icons/
    - assets/models/
